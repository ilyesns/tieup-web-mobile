enum TypeUser {
  // Define enum TypeUser
  admin,
  user,
}

enum Role {
  // Define enum Role
  freelancer,
  client,
}

enum ExperienceLevel {
  beginner,
  intermediate,
  expert,
}

enum OfferStatus {
  pending,
  active,
  completed,
}

enum OrderPaymentStatus {
  Unpaid,
  Paid,
}

enum OrderStatus {
  pending,
  inProgress,
  delivered,
  completed,
  cancelled,
  Late,
}

enum MessageStatus { pending, accepted, rejected }
